@model Students.WebApp.ViewModels.Account.LoginModel
@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}

<div class="login-box">
    <div class="login-box-body">
        <p class="login-box-msg">Login</p>
        <form method="post" asp-controller="Account" asp-action="Login">
         @*    @Html.ValidationSummary(true) *@
@*             <div asp-validation-summary="ModelOnly" class="text-danger"></div> *@
            <div asp-validation-summary="All"></div>
            <fieldset>
                <legend></legend>
@*                 <div class="row">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(err => err.Errors))
                    {
                        <div class="alert alert-warning" role="alert">
                            &nbsp;@error.ErrorMessage
                        </div>
                    }
                </div> *@
                <div class="form-group has-feedback">
                    <input asp-for="UserName" class="form-control" type="text" placeholder="User" />
                 @*    @Html.TextBoxFor(m => Model.UserName, new { @placeholder = "User", @class = "form-control" }) *@
                    <span class="fa fa-user form-control-feedback"></span>
                    @* <span asp-validation-for="UserName" class="alert alert-danger" /> *@
                </div>
                <div class="form-group has-feedback">
                    <input asp-for="Password" class="form-control" type="password" placeholder="Password" />
                    @* @Html.PasswordFor(m => Model.Password, new { @placeholder = "Password", @class = "form-control" }) *@
                    <span class="fa fa-lock form-control-feedback"></span>
                </div>
                <div class="body">
                    <div class="footer">
                        <button type="submit" id="login" class="btn btn-primary btn-block btn-flat">Login</button>
                    </div>
                </div>
            </fieldset>
        </form>
    </div>
</div>

@section Scripts {
    <script src="~/lib/jquery.unobtrusive-ajax.js"></script>
    <script src="~/lib/jquery.validate.js"></script>
    <script src="~/lib/jquery.validate.unobtrusive.js"></script>
}
